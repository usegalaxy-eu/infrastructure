resource "openstack_compute_instance_v2" "training-TRAINING_IDENTIFIER" {
  name            = "vgcnbwc-training-TRAINING_IDENTIFIER-${count.index}"
  image_name      = "${var.vgcn_image}"
  flavor_name     = "VM_SIZE"
  key_pair        = "cloud2"
  security_groups = ["public"]
  count           = VM_COUNT

  user_data = "${file("conf/node.yml")}"

  network {
    name = "bioinf"
  }

  provisioner "remote-exec" {
    when = "destroy"

    scripts = [
      "./conf/prepare-restart.sh",
    ]

    connection {
      type        = "ssh"
      user        = "centos"
      private_key = "${file("~/.ssh/keys/id_rsa_cloud2")}"
    }
  }
}
